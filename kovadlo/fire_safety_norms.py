"""
Нормативні дані для розміщення пожежних датчиків.

Орієнтовні значення поширеної практики (структура на кшталт вимог
**EN 14604** для автономних димових сповіщувачів і загальних принципів
розміщення з національних будівельних норм) — площа покриття одного
датчика, максимальна відстань між датчиками й мінімальний відступ від
стін. Це узагальнені орієнтири для попереднього розрахунку, а НЕ
дослівна виписка з норми — реальне розміщення завжди перевіряйте за
чинними будівельними нормами й інструкцією виробника датчика.
"""

from __future__ import annotations

from enum import Enum


class DetectorKind(Enum):
    """Тип пожежного датчика."""

    SMOKE = "димовий"
    HEAT = "тепловий"
    CO = "чадного газу"


# Орієнтовна площа покриття одного датчика, м².
COVERAGE_AREA_M2: dict[DetectorKind, float] = {
    DetectorKind.SMOKE: 40.0,
    DetectorKind.HEAT: 40.0,
    DetectorKind.CO: 40.0,
}

# Орієнтовна максимальна відстань між сусідніми датчиками, м.
MAX_SPACING_M: dict[DetectorKind, float] = {
    DetectorKind.SMOKE: 9.0,
    DetectorKind.HEAT: 7.5,
    DetectorKind.CO: 9.0,
}

# Орієнтовний мінімальний відступ датчика від стіни/кута, м.
WALL_CLEARANCE_M: dict[DetectorKind, float] = {
    DetectorKind.SMOKE: 0.5,
    DetectorKind.HEAT: 0.5,
    DetectorKind.CO: 0.5,
}
